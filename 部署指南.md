# 部署指南

## 一键部署到 GitHub

### 步骤 1：双击运行 setup-github.bat

在项目文件夹中双击 `setup-github.bat`，按提示操作。

### 步骤 2：创建 GitHub 仓库

1. 打开 https://github.com/new
2. 填写仓库信息：
   - Repository name: `plc-program-assistant`
   - Description: `掌上移动PLC编程助手 - H5在线版`
   - 选择 Public 或 Private
   - ❌ 不要勾选 "Add a README file"
3. 点击 "Create repository"

### 步骤 3：复制仓库地址

创建成功后，复制仓库的 HTTPS 地址，类似：
```
https://github.com/你的用户名/plc-program-assistant.git
```

### 步骤 4：粘贴到脚本中

将地址粘贴到提示处，脚本会自动推送代码。

### 步骤 5：启用 GitHub Pages

1. 进入你的 GitHub 仓库
2. 点击 **Settings**（设置）
3. 左侧菜单选择 **Pages**
4. Source 选择 **GitHub Actions**
5. 等待自动部署完成（约2-3分钟）

### 步骤 6：访问你的应用

部署完成后，访问：
```
https://你的用户名.github.io/plc-program-assistant
```

---

## 手动部署（不需要脚本）

如果你想手动操作：

```bash
# 1. 进入项目目录
cd D:\AI自动化\PLC编程助手小程序

# 2. 初始化Git
git init

# 3. 添加文件
git add .

# 4. 提交
git commit -m "Initial commit"

# 5. 添加远程仓库
git remote add origin https://github.com/你的用户名/plc-program-assistant.git

# 6. 推送
git push -u origin main --force
```

然后在 GitHub 仓库 Settings > Pages 中启用。

---

## 微信中打开

部署成功后，在微信中打开链接即可使用：

1. 复制链接
2. 发送到文件传输助手
3. 点击打开

或者：
1. 微信搜索栏粘贴链接
2. 选择在浏览器中打开

---

## 常见问题

### Q: 部署后页面空白？
A: 检查浏览器控制台是否有错误，可能是路径问题。确保使用 GitHub Actions 方式部署。

### Q: 微信打不开？
A: 微信可能限制了外部链接，建议先在手机浏览器测试。

### Q: 想用自己的域名？
A: 在 GitHub Pages 设置中可以绑定自定义域名。

---

## 目录结构

```
D:\AI自动化\PLC编程助手小程序\
├── .github/
│   └── workflows/
│       └── deploy.yml     # 自动部署配置
├── src/                   # 源代码
├── dist/                  # 构建输出
├── package.json
├── setup-github.bat       # 一键部署脚本
├── README.md
└── 部署指南.md
```
